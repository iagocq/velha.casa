extends /layout

mixin site(link, name, proto="https://", index="/")
  li
    =name
    |: 
    +link(href=`${proto}${link}${index}`)= link

append content
  +h1("outros sites")
  p Sites de amigos:
  ul
    +site("firmwarejun.net", "Jun")
    +site("狼.net", "Matt")
